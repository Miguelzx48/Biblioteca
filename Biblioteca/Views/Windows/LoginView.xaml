<Window x:Class="Biblioteca.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoginView"
        Height="450"
        Width="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Grid Background="#A394F7">

        <Border Background="White" CornerRadius="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20"
                                  ShadowDepth="5"
                                  Opacity="0.3"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" CornerRadius="15,0,0,15">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#0062E6"/>
                            <GradientStop Color="#33AEFF" Offset="0.5"/>
                            <GradientStop Color="#121831"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <Grid Margin="30">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>



                        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
                            <TextBlock Text="Hola,"
                                       Foreground="White"
                                       FontSize="36"
                                       FontWeight="Light"/>

                            <TextBlock Text="Bienvenido!"
                                       Foreground="White"
                                       FontSize="42"
                                       FontWeight="Bold"
                                       Margin="0,-5,0,0"/>

                            <TextBlock Text="Sistema de gestión de biblioteca"
                                       Foreground="White"
                                       TextWrapping="Wrap"
                                       Margin="0,15,0,0"
                                       FontSize="13"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Column="1" Margin="40">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" VerticalAlignment="Center">

                        <TextBlock Text="Email"
                                   Foreground="#888"
                                   FontSize="12"/>

                        <Border Background="#F4F6F9"
                                CornerRadius="8"
                                Padding="10"
                                Margin="0,5,0,15">
                            <TextBox x:Name="txtEmail"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Height="25"/>
                        </Border>

                        <TextBlock Text="Contraseña"
                                   Foreground="#888"
                                   FontSize="12"/>

                        <Border Background="#F4F6F9"
                                CornerRadius="8"
                                Padding="10"
                                Margin="0,5,0,15">
                            <PasswordBox x:Name="txtPassword"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Height="25"/>
                        </Border>

                        <Button Content="Acceder"
                                Height="40"
                                Background="#4A65FF"
                                Foreground="White"
                                FontWeight="Bold"
                                Click="Button_Click"
                                BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="10"
                                            SnapsToDevicePixels="True">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="6"
                                                              ShadowDepth="2"
                                                              Opacity="0.2"/>
                                        </Border.Effect>

                                        <ContentPresenter HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            

                        </Button>
                        <TextBlock HorizontalAlignment="Center"
                                   Margin="0,10,0,15"
                                   FontSize="13">

                        <Hyperlink Click="Hyperlink_Click"
                                       Foreground="#FF0079FF">
                                ¿Olvidaste tu contraseña?
                         </Hyperlink>    

                        </TextBlock>

                        <Label x:Name="lblError"
                               Foreground="Red"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
                        <TextBlock Text="¿Aún no estás registrado?"
                                   Foreground="#888"
                                   HorizontalAlignment="Center"
                                   FontSize="12"
                                   Margin="0,0,0,10"/>
                        <Button Content="Registrate"
                                Height="36"
                                Foreground="White"
                                FontWeight="Bold"
                                Click="Button_Click_1"
                                BorderThickness="0">

                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="10">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#37C5EC"/>
                                                <GradientStop Color="#3F76FA" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>

                                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>

                        </Button>

                        <Button Content="Salir"
                                Height="28"
                                Margin="0,5,0,0"
                                Background="Transparent"
                                Foreground="Gray"
                                BorderThickness="0"
                                Click="Button_Click_2"/>
                    </StackPanel>

                </Grid>
            </Grid>
        </Border>
    </Grid>
    
</Window>